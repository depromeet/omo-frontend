// 사진 수정 페이지
import styled from 'styled-components';

import Layout from '@components/Layout';

const changePhoto = () => {
  return <Layout title="프로필사진 변경">프로필사진 변경</Layout>;
};

export default changePhoto;
